# Resume Placeholder

Place your resume PDF here with the exact filename:

`Rohan-Mondal-Resume.pdf`

The "Download Resume" button in the Hero section will automatically become enabled once the file exists.

To enable the button, also set `RESUME_EXISTS = true` in:
`src/components/Hero/Hero.jsx`
